<form method="post" action="{{ path('app_linea_factura_delete', {'id': linea_factura.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ linea_factura.id) }}">
    <button class="btn">Delete</button>
</form>
