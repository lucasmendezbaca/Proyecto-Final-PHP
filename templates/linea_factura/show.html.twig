{% extends 'base.html.twig' %}

{% block title %}LineaFactura{% endblock %}

{% block body %}
    <h1>LineaFactura</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ linea_factura.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ linea_factura.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_linea_factura_index') }}">back to list</a>

    <a href="{{ path('app_linea_factura_edit', {'id': linea_factura.id}) }}">edit</a>

    {{ include('linea_factura/_delete_form.html.twig') }}
{% endblock %}
